import { Routes } from "@angular/router";
import { CalculadoraComponent } from "./service";

export const CalculadoraRoutes : Routes = [
    {
        path: 'calculadora',
        component: CalculadoraComponent
    }
]